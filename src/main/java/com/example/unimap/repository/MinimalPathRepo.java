package com.example.unimap.repository;

import com.example.unimap.entity.MinimalPath;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MinimalPathRepo extends JpaRepository<MinimalPath, String> {
}
